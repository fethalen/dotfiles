# Don't ask for my CRAN mirror
options(repos = c(CRAN = "https://stat.ethz.ch/CRAN/"))

# Eliminate the “save workspace image” prompt on exit
options(save.workspace = "no")

options(cli.num_colors = 256)

options(editor = "vim")
